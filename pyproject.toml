[project]
name = "ridgeline-freight-accrual"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "pandas>=2.0",
  "openpyxl>=3.1",
  "pillow>=10.0"
]

[tool.vercel]
entrypoint = "api.regenerate:handler"
